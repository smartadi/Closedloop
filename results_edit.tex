% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section*{Results}
% %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

We developed a real-time closed-loop optogenetic system to regulate population activity within a targeted cortical region, monitored using wide-field calcium imaging. The trial-averaged cortical response to optogenetic stimulation was well approximated by a stationary linear input--output map, providing the empirical foundation for controller design. 

% ------------------------------------------------------------
\subsection*{Experimental setup and closed-loop control architecture}
% ------------------------------------------------------------ 
We designed a closed loop system which used real-time feedback from cortical regions and applied targeted stimulation. 
The closed-loop experiments were performed in head-fixed transgenic GCaMP and opsin-expressing mice in a behavioral chamber \citep{Matveev2024}. The front limbs of the mice rested on a freely moving wheel, and a blank screen was present in front of the mouse.
Neural activity was recorded at 35\,Hz using wide-field calcium imaging(Fig.~\ref{fig:figure1}A), and spatially resolved optogenetic stimulation was applied to a targeted cortical region neighboring the $\Delta F/F$ output zone as depicted in Fig.~\ref{fig:figure1}B. The stimulation region was picked to be close to the output kernel(Figure~\ref{fig:figure1}C) so as to directly inhibit the regions activity. 
We then designed a hardware and software architecture for signal processing and fast closed-loop control of this mesoscale activity (Fig.~\ref{fig:figure1}D). The software layer handled the mathematical framework required for computing control signal through feedback.
A proportional--integral (PI) feedback controller with open-loop compensation was designed to drive cortical activity toward the reference $ -5\%\,\Delta F/F$(Figure~\ref{fig:figure1}E).
The open-loop component provided the nominal stimulus required to reach a reference state based on the calibrated controller gains, while the PI feedback component added real-time error correction to compensate for bias due to inaccurate open loop gains, residual nonlinearities, and spontaneous trial-to-trial fluctuations.
The control system as depicted in Figure~\ref{fig:figure1}E, treats the kernel averaged wide-field measurements as outputs, the controller uses the error between output and reference signal to compute an optogenetic stimulus input to the plant, subject to a processing and physiological delay that closes the loop (Fig.~\ref{fig:figure1}E). The system has an inherent maximum latency of 47ms computed by determining the time between signal arrival at cortical level to the time of application of laser at the cortical level. \aditya{Moreover, there exists a physiological delay that accounts for the time of application of laser to its effects affecting the calcium imaging output}.  

\begin{figure}[h]
    \centering
    \includegraphics[width = \linewidth]{images/Figure1.pdf}
    \caption{Experimental setup and Closed-loop control architecture.\\
    \small{
     \textbf{A:}Schematic of the microscope optical path. Blue and violet LEDs reflect off the first dichroic toward the skull. Emitted green photons pass through the first dichroic and reflect off the second dichroic to the camera. A red (638 nm) laser is steered with a two-mirror galvanometer and passes through both dichroics to the brain.\\
    \textbf{B:} Schematic of mouse brain showing stimulation region and output region.\\
    \textbf{C:} A single frame of Widefield image showing the optogenetic stimulation spot in red and the kernel considered as output with blue box.\\
    \textbf{D:}The Signal-flow pipeline showing Hardware and Software layer of the system. The signal flow is triggered by a trigger that initiates widefield image capture, the Image API receiving the data and software processes the image and generates the output signal using controller layer. The control signal is then sent to optogenetic hardware for laser activation.
    \textbf{E:} A representation of control system architecture which shows reference signal tracking. The output signal is dF/F value of the spatial average of output region. The given reference signal is tracking using output feedback as shown by computation of error signal. The controller used error signal to compute the closed-loop response and the total input is computed by adding the open-loop signal. The input signal is applied using optogenetic hardware to the system which is influenced by exogenous inputs and brain-state parameters.\\
    \textbf{F:}  Histogram showing distribution of system loop times representing the latency of closed loop architecture. The latency represents the time between signal affecting cortical activity to the time of application of corresponding laser signal to the cortex.
    }}
    \label{fig:figure1}
\end{figure}

% ------------------------------------------------------------
\subsection*{Trial-averaged cortical dynamics are approximated by a linear system}
% ------------------------------------------------------------
Trial-averaged cortical responses were well approximated by a linear, stationary input--output map across both impulse and step stimulus types.
We delivered impulse-based inputs at varying amplitudes with randomized inter-trial intervals, and found that trial-averaged peak inhibition energy scaled monotonically with stimulation power across three independent sessions (Fig.~\ref{fig:figure1}A. The inhibition energy which is quantified as the output had a linear relation with the input impulse amplitude with MSE of $R^{2} =  \mathbf{[0.91,0.85,0.93]}$ and slope $= \mathbf{[-0.28,-0.50,-0.64]}$ across the three sessions. This prompts to the fact that a linear map well approximates the open loop behavior, and therefore a open loop controller of the form \ref{eq:ff} can be designed. We also observe that a linear dynamical system can satisfactorily explain the behavior of input-output dynamics by fitting a transfer function to the impulse data.[\aditya{add the figure as supplimentary}]

\begin{figure}
    \centering
    \includegraphics[width = \linewidth]{images/Figure2.pdf}
    \caption{Input-output characterization of local cortical dynamics.\\
    \small{
     \textbf{A:}% Averaged impulse responses across input powers.
    Trial-averaged impulse response for a given laser power in mW. The response(dF/F) is shown relative to the activity at the time of stimulation shown in red. Laser power was randomly sampled for each trial.    
    \\
    \textbf{B:}% Trial-averaged peak inhibition vs.\ stimulation amplitude across sessions.\\
    Relationship between Impulse Stim to peak output for increasing power values for three sessions. For each session, the plot shows mean response and std-dev bar per input power. The lines depict the fitted input-output relationship\\     
    \textbf{C:}% Trial-averaged step response across sessions.
    Response to step input Stim condition across three sessions, the stimulation window is shown in gray. The solid lines represent the mean response across the Stim window. The traces overlayed on top show the trial-by-trial variance as a function of time, per session.}}
    \label{fig:figure2}
\end{figure}

Step inputs applied over 3-second trial windows produced trial-averaged trajectories that remained within a bounded set around the session mean (Fig.~\ref{fig:figure1}D), consistent with marginally stable oscillatory dynamics in biologically bounded cortical circuits.
Stimulation onset did not measurably alter trial-to-trial variance (Fig.~\ref{fig:figure1}D), indicating that optogenetic input did not destabilize the system.
Session-to-session offset bias in the step response informed inclusion of the integral term in the PI controller (see Materials and Methods).
Batch-mean $\Delta F/F$ variance during spontaneous activity decreased monotonically with trial count, stabilizing beyond approximately 50 trials per session (Fig.~\ref{fig:S1}), consistent with the trial-averaging assumption (see Materials and Methods).
Convergence rates differed across sessions recorded from the same region, consistent with session-to-session differences in brain state.

% ------------------------------------------------------------
\subsection*{Closed-loop control improves reference tracking accuracy}
% ------------------------------------------------------------

Across sessions in two mice with confirmed opsin expression, closed-loop feedback consistently improved tracking of $y_{\mathrm{ref}} = -5\%\,\Delta F/F$ relative to open-loop stimulation (Fig.~\ref{fig:figure3}A--D). 
Under open-loop conditions, the feedforward term produced cortical responses that systematically deviated from the reference, reflecting the inability of a fixed input to compensate for ongoing spontaneous fluctuations or inaccuracies in the estimated gain $K_{r}$.
Under closed-loop control, the proportional term corrected instantaneous deviations while the integral term accumulated and eliminated persistent offset, yielding substantially improved alignment with $y_{\mathrm{ref}}$ at the single-trial level (Fig.~\ref{fig:figure3}A).

\begin{figure}
    \centering
    \includegraphics[width = \linewidth]{images/Figure3.pdf}
    \caption{Closed-loop feedback controller analysis\\
    \small{
    \textbf{A:} A comparison between random trial that implemented open-loop(red) and closed-loop(green) controllers. The shaded gray region shows the stimulation window during which the reference dF/F state is set to -5dF/F, represented by the dashed line. The input traces are overlayed on Stim Windows. 
    \textbf{B:} Comparison of trial-averaged activity under both control scenarios. The bold lines show the trial averaged activities whereas the thin lines show the individual traces.  
    Trial-averaged activity across conditions.\\
    \textbf{C:} Comparison of input activity for open-loop(pink) and closed-loop(blue) controllers, during the stim window. Single trial inputs are represented with thin lines and trial-averaged input is shown with bold traces.\\
    \textbf{D:} Comparison of variance across trials as a function of time for both control scenarios.\\
    \textbf{E:} Comparison of distribution of tracking MSE per trial for the control scenarios shown using violin plot.\\ 
    \textbf{F:} Variance averaged across sessions.\\
    \textbf{H:} Average of tracking error per unit time for each controller across experiments. The trace in bold shows average across all experiments.
    \textbf{G:} Per-trial MSE distributions across sessions.
    }}
    \label{fig:figure3}
\end{figure}

Trial-averaged responses confirmed that closed-loop activity followed the reference closely throughout the stimulation window, whereas open-loop responses exhibited systematic positive bias (Fig.~\ref{fig:figure3}B).
The difference in average input responses between conditions (Fig.~\ref{fig:figure3}C) reflects the additional corrective signal contributed by real-time feedback. 
The distribution of per-trial tracking costs was shifted toward lower values and exhibited reduced spread under closed-loop control (Fig.~\ref{fig:figure3}E), indicating both improved mean performance and enhanced reliability of neural modulation across trials.
These improvements were consistent across all recorded sessions (Fig.~\ref{fig:figure3}G) and were achieved with modest gains ($K_{p} = \mathbf{[X]}$, $K_{i} = \mathbf{[X]}$), suggesting that the approximately linear, stationary input--output structure identified empirically was sufficient to support stable PI feedback.

% ------------------------------------------------------------
\subsection*{Closed-loop control reduces trial-to-trial variability}
% ------------------------------------------------------------
In addition to improving mean tracking accuracy, closed-loop feedback substantially reduced trial-to-trial variability in cortical activity during the stimulation window (Fig.~\ref{fig:figure3}D--F). 
This result follows from the structure of the PI controller operating under the trial-averaging assumption: individual trial trajectories are perturbations of the nominal averaged response driven by zero-mean disturbances $d_{t}$ and parameter fluctuations $\rho_{t}$, and the controller actively steers each trial back toward $y_{\mathrm{ref}}$, thereby compressing the cross-trial distribution of responses.

During open-loop stimulation, response variance remained largely unchanged relative to the pre-stimulation baseline, reflecting the inability of the fixed feedforward term to counteract spontaneous fluctuations. Under closed-loop control, variance decreased substantially during the stimulation window, consistent with active disturbance rejection. This reduction persisted across sessions, as confirmed by pooled within-session variance traces (Fig.~\ref{fig:figure3}E--F) and by the cross-session distribution of per-trial MSE (Fig.~\ref{fig:figure3}G).

The validity of these results depends on the trial-averaging assumption holding across the session. The empirical variance convergence in Fig.~\ref{fig:S1} supports this assumption over the 100-trial validation period. 
Performance varied across animals and sessions, likely reflecting residual non-stationarity in arousal and behavioral state, differences in opsin expression, and session-to-session variation in the spatial overlap between stimulation and output regions.
Despite this variability, directional improvements in both tracking accuracy and trial-to-trial variance were consistent across all experiments.
